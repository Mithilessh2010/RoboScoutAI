def extract_frames(video_path: str, every_n_seconds: float = 0.5) -> list[dict]:
    """Placeholder for OpenCV frame extraction."""
    return [
        {
            "frame_id": "placeholder-0001",
            "timestamp": 0.0,
            "video_path": video_path,
            "sampling_seconds": every_n_seconds,
        }
    ]
