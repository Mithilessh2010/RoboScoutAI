# Run Status

Current target architecture: one SvelteKit/Vercel app owns both frontend and backend routes.

Use:

```sh
npm run web:dev
npm run web:build
```

Relevant local routes:

- `/graphql`
- `/api/ftc/index`
- `/api/autoscore/jobs`
- `/watch`
